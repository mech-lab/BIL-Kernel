# Phase 5 Fixture Keys

These keys are deterministic, non-production Ed25519 fixtures for the committed Phase 5 interoperability examples.

They exist only so the example bundles and receipts can be regenerated exactly and verified end to end inside the repository.

They must never be reused for real signing, trust, or deployment workflows.
